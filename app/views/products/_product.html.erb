<div class="flex w-1/4 flex-wrap">
  <div class="w-full p-1 md:p-2">
    <div id="<%= dom_id product %>">
      <p class="my-5">
        <%= product.name %>
      </p>

      <p class="my-5">
        <%= product.description %>
      </p>

      <p class="my-5">
        <%= '%.2f' % product.price %>
      </p>

      <% if action_name != "show" %>
        <%= link_to "Show this product", product, class:"inline-block rounded bg-neutral-800 px-6 pb-2 pt-2.5 text-xs font-medium uppercase leading-normal text-neutral-50" %>
        <hr class="mt-6">
      <% end %>

      <div class="relative mb-3">
        <%= form_with url: buy_path, method: :get do |form| %>
          <%= form.hidden_field :product_id, value: product.id %>
          <%= form.number_field :amount, class:'w-[150px]' %>
          <%= form.submit "Buy", class:"inline-block rounded bg-neutral-800 px-6 pb-2 pt-2.5 text-xs font-medium uppercase leading-normal text-neutral-50" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
